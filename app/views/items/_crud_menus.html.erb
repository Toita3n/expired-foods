<%= link_to edit_item_path(item), class: "line-center", id: "button-edit-#{item.id}" do %>
  <%= icon 'fa', 'pen' %>
<% end %>
 |
<%= link_to item_path(item), class: "line-center", id: "button-delete-#{item.id}", method: :delete, data:{confirm: t('defaults.message.deleted_confirm')} do %>
  <%= icon 'fas', 'eraser' %>
<% end %>