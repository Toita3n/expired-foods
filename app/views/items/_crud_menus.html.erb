<ul class='crud-menu-btn list-inline float-right'>
  <li class="list-inline-item">
    <%= link_to edit_item_path(item), id: "button-edit-#{item.id}" do %>
     <%= icon 'fa', 'pen' %>
    <% end %>
  </li>
|
  <li class="list-inline-item">
    <%= link_to item_path(item), id: "button-delete-#{item.id}", method: :delete, data:{confirm: 'Are you sure?'} do %>
      <%= icon 'fas', 'trash' %>
    <% end %>
  </li>
</ul>