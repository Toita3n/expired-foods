<% content_for(:title, t('.title')) %>
<div class="box-password-reset">
 <div class="row">
   <h1><%= t('.title')%></h1>
   <p>入力されたメールアドレスにパスワード変更のURLを送信します。</p>
   <%= form_with url: password_resets_path, local: true, method: :post do |form| %>
     <div class="field">
       <%= t('.email') %>
       <%= form.text_field :email %>
       <%= form.submit t('defaults.submit'), class:"btn btn-primary" %>
     </div>
   <% end %>
  </div>
</div>