<%= form_with model: @search_tags_form, scope: :q, url: search_admin_tags_path, method: :get, local: true do |f| %>
  <%= f.search_field :name, class: "form-control me-2", placeholder: "タグ名" %>
  <%= f.submit '検索', class: 'btn btn-outline-primary' %>
<% end %>